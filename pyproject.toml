[project]
name = "tinyforgelm"
version = "0.1.0"
description = "TinyForgeLM tokenizer/data utilities."
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0.0",
    "pyyaml>=6.0.0",
    "torch>=2.12.1",
    "transformers>=4.52.0",
]

[tool.uv]
package = false
